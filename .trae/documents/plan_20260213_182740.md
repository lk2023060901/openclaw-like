## 修复计划

### 问题 2: 私聊 workspace 路径优化

**修改文件**: `extensions/feishu/src/dynamic-agent.ts`

- 添加 `userName` 参数到 `maybeCreateDynamicAgent`
- 当用户名可用时，创建更友好的 agentId：
  - 原格式: `feishu-ou_xxxxx`
  - 新格式: `feishu-{userName}-{shortId}` 或 `feishu-{userName}`
- workspace 路径也使用友好名称

### 问题 3 & 4: 用户 ID 和发送者显示优化

**修改文件**: `extensions/feishu/src/bot.ts`

- 改进 `resolveFeishuSenderName` 函数：
  - 添加更多回退选项（union_id, user_id）
  - 缓存时间延长
  - 更好的错误处理
- 改进显示逻辑：
  - 优先使用 `senderName`
  - 回退到 `senderId`（比 openId 更短）
  - 最后才用 `senderOpenId`

### 日志显示改进

- 日志中同时显示用户名和 ID：`张三 (ou_xxxxx)`
- 消息体中的发送者显示更友好

### 配置项

- 添加 `displayNameFallback` 配置，允许自定义回退显示格式
